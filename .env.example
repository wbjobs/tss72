# 环境配置
PORT=3000
NODE_ENV=development

# PostgreSQL 数据库连接
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rls_middleware
DB_USER=postgres
DB_PASSWORD=postgres
DB_SCHEMA=public

# JWT 配置（可选，用于用户认证）
JWT_SECRET=your-super-secret-jwt-key-change-in-production
